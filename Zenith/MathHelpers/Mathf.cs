using System;

namespace Zenith.MathHelpers
{
    public class Mathf
    {
        internal static float PI = (float)Math.PI;

        internal static float Cos(float x) { return (float)Math.Cos(x); }
        internal static float Sin(float x) { return (float)Math.Sin(x); }
    }
}
